import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.newinstagramclone.app',
  appName: 'new-instagram-clone',
  webDir: 'dist'
};

export default config;
